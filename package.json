{
  "name": "nodejs-basics",
  "version": "1.0.0",
  "description": "Simple web server using basics of nodejs to output a page with form to create users and view them on different page",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
